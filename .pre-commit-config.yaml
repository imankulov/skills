repos:
  - repo: local
    hooks:
      - id: generate-skill-metadata
        name: Generate skill repository metadata
        entry: uv run --script scripts/generate_marketplace.py
        language: system
        pass_filenames: false
        always_run: true
